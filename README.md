# Count files size

Script to count the size of files recursively from a specified path.

The goal is just to practice *Binary Tree* fundamentals by using a BST to store
and sort files by size.

## How to run

```sh
./gradlew run --args="/path/to/count"
```
